現在の Arduino valve_controller 実装は dry-run まで完了しており、土壌水分センサーの暫定校正値でも動作確認済みです。

今回の修正目的は、コードの大改修ではなく、設計資料とREADMEに「Arduinoは水やり判断をしない」「定期観測とAI判断タイミングを分離する」方針を明確に反映することです。

重要な方針:
- 土壌が乾燥していても、ArduinoやRaspberry Piの固定ロジックだけで自動的に水やりしない
- 水をあげるかどうかは、朝・夕方の判断タイミングでAIエージェントが判断する
- Arduinoは water <duration_ms> 命令を受けた場合に、安全に実行できるかだけを判定する
- Arduinoの wet判定、単回最大開放時間、1日累積上限は「判断」ではなく「安全拒否」
- 定期的な土壌水分取得は、データ蓄積・データレイク目的で行う
- 定期観測と水やり判断を分離する

対象ファイル:
- edge/arduino/valve_controller/README.md
- docs/03_system_design.md
- docs/04_detail_design.md
- docs/05_data_design.md
- docs/06_operations.md
- 必要なら README.md / CODE.md

実装コードについて:
- valve_controller.ino / sensor.h / safety.h / valve.h の安全ロジックは現状維持でよい
- 乾燥時に自動で water を実行するような処理は絶対に追加しない
- read/status/close/water のシリアルコマンド構成は維持する
- ArduinoJsonなど外部ライブラリは追加しない
- Raspberry Pi/GCP/AI判断の実装は今回は追加しない

# 1. Arduino README の追記

edge/arduino/valve_controller/README.md に、以下の趣旨を明記してください。

追記内容:
- Arduino は「水をあげるべきか」は判断しない
- Arduino は Raspberry Pi / AIエージェントから `water <duration_ms>` 命令を受けた場合に、安全に実行できるかだけを判定する
- 土壌が乾燥していることは water 実行を許可しうる条件であり、自動水やりのトリガーではない
- wet判定は「湿りすぎなら拒否する」ための安全ロジック
- 単回最大開放時間と1日累積上限も安全ロジック
- 将来、Raspberry Piは定期的に `read` を実行して土壌水分を記録する
- 朝・夕方の判断タイミングのみ、AIエージェントの判断結果に従って `water <duration_ms>` を送る

README内の「役割」または「注意点」に入れてください。

# 2. docs/03_system_design.md の修正

Raspberry Pi / Arduino / Cloud Run の責務分離に、以下を追加してください。

- Raspberry Pi は定期観測スケジューラを持つ
- 定期観測では Arduino の `read` を呼び、土壌水分 raw / percent を取得して保存する
- 定期観測はデータ蓄積目的であり、水やり実行の直接トリガーではない
- AI判断は毎回の観測ではなく、原則として朝・夕方の decision window でのみ行う
- 判断時刻以外の観測では `POST /judge` を呼ばない
- ただし、将来的に緊急レビュー用の alert は検討可能だが、初期実装では自動水やりに直結させない
- Arduino は AI判断をしない。受け取った `water <duration_ms>` の安全実行可否だけを判定する

# 3. docs/04_detail_design.md の修正

現在の通常観測シーケンスが「観測後にAIグループならPOST /judgeを呼ぶ」ように読める場合、以下のように分離してください。

## 通常観測シーケンス

1. Raspberry Pi が現在時刻を確認する
2. カメラ撮影が必要な観測タイミングなら画像を撮影する
3. Arduino に `read` を送り、土壌水分 raw / percent を取得する
4. 必要なら天気を取得する
5. `observations` または `soil_moisture_readings` に保存する
6. 通常観測では水やりを実行しない

## AI判断シーケンス

1. 朝・夕方の decision window でのみ実行する
2. 最新の観測、直近履歴、画像、天気を agent-api の `POST /judge` に送る
3. AIが `water` / `observe_only` / `manual_review` などをJSONで返す
4. `ai_decisions` に保存する
5. `action=water` の場合のみ、Raspberry Pi が mL を duration_ms に変換する
6. Raspberry Pi が Arduino に `water <duration_ms>` を送る
7. Arduino が wet判定、単回上限、日次上限で安全判定する
8. 実行結果または拒否結果を `watering_events` に保存する
9. 成功時のみ、水やり後5分間隔の効果測定を開始する

## 定期土壌水分測定シーケンス

1. Raspberry Pi が一定間隔で Arduino に `read` を送る
2. 土壌水分 raw / percent を取得する
3. データ蓄積目的で保存する
4. 乾燥していても、このシーケンス単独では水やりしない

# 4. docs/05_data_design.md の補足

定期観測と水やり後測定の保存先を整理してください。

方針:
- 画像・天気・土壌水分を含む観測スナップショットは `observations`
- 土壌水分のみの時系列ログは `soil_moisture_readings`
- 水やり後5分間隔の効果測定も `soil_moisture_readings`
- AI判断に使った観測は `ai_decisions.observation_id` で参照する
- 判断時刻以外の土壌水分ログは、AI判断に直結せず、履歴・分析・データレイクとして利用する

必要に応じて `measurement_phase` の列挙値を見直してください。

候補:
- `normal_periodic`
- `decision_baseline`
- `post_watering`
- `manual_check`

既存の `normal_hourly` がある場合は、意味を明確にするだけでもよいです。

# 5. docs/06_operations.md の補足

運用手順に以下を追加してください。

- 通常時は定期的に土壌水分が記録されているか確認する
- 朝・夕方のAI判断が実行されたか確認する
- 判断時刻以外の乾燥値だけで水やりが実行されていないことを確認する
- 水やりが実行された場合のみ、5分間隔の効果測定が始まることを確認する
- 実験外時間は手動バルブを閉じる運用を維持する

# 6. README.md / CODE.md の補足

必要であれば、全体方針に以下を追加してください。

- 観測と判断を分離する
- 定期観測はデータ蓄積目的
- 水やり判断は朝・夕方のAIエージェント判断のみ
- Arduinoは安全制御装置であり、自律的な栽培判断はしない

# 7. 完了条件

- 設計資料上、「乾燥していたら固定ロジックで自動水やりする」ように読める記述をなくす
- 通常観測、AI判断、水やり実行、効果測定の4つの流れが分離されている
- Arduino READMEに「Arduinoは水やり要否を判断しない」と明記されている
- 既存のArduino安全ロジックは弱めない
- 乾燥状態を自動水やりトリガーにするコードは追加しない
- Raspberry Pi/GCP/AI判断の実装は今回は追加しない
- 人間側データ保存につながる処理は追加しない